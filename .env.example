# CrewForge environment configuration
# Copy this file to `.env` and adjust as needed. All values are optional.

# Port the local server listens on (default: 8787).
PORT=8787

# API keys are NOT configured here. They are entered in the app's Setup page
# and stored encrypted in the local SQLite database. Keys are never written to
# logs, SSE events, or run history.
